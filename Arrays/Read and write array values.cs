// set the first item in the array to 'Han Solo'. On the next line, write the first item in the array to the Console.
using System;

public class Readandwritearrayvalues
{
    public static void Main()
    {
        string[] names = new string[1];
    		names[0] = "Han Solo";
    		Console.WriteLine(names[0]);
    }
}
